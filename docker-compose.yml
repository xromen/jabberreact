services:
  jabber-react:
    build: .
    ports:
      - "${JABBER_HTTP_PORT:-18080}:8080"
    environment:
      DEFAULT_XMPP_SERVER: ""
      DEFAULT_XMPP_HTTP_PORT: "7070"
      DEFAULT_XMPP_HTTP_PATH: "/http-bind/"
    restart: unless-stopped
